// This component is no longer used in the new design.
// The hero now uses QuoteFormDialog triggered by CTA buttons.
// Keeping file for backward compatibility.
const HeroQuoteForm = () => null;
export default HeroQuoteForm;
